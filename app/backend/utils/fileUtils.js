const fs = require('fs');

const readData = (file) => {
    // Lógica de lectura de datos
};

const writeData = (file, data) => {
    // Lógica de escritura de datos
};

module.exports = { readData, writeData };
